!#bin/bash
tar cpf /home/kali/study/bachup "created on `date '+%d-%B-%Y'`.tar" /home
tar cpf /home/kali/study/bachup "created on `date '+%d-%B-%Y'`.tar" /etc/vsftpd.conf
tar cpf /home/kali/study/bachup "created on `date '+%d-%B-%Y'`.tar" /etc/ssh/sshd_config
tar cpf /home/kali/study/bachup "created on `date '+%d-%B-%Y'`.tar" /etc/xrdp/startwm.sh
tar cpf /home/kali/study/bachup "created on `date '+%d-%B-%Y'`.tar"  /etc/xrdp
tar cpf /home/kali/study/bachup "created on `date '+%d-%B-%Y'`.tar"  /var/log
